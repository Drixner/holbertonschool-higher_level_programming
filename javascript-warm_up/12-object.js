#!/usr/bin/node
// Update this script to replace the value 12 with 89
const myObject = {
  type: 'object',
  value: 12
};
console.log(myObject);
myObject.value = 89; // se modifica el valor del objeto
console.log(myObject);
